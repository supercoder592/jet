# 多模式設計（討論用，尚未實作）

開賽後隨機抽一個作戰模式，每個模式都有自己的勝負條件、目標 HUD 與長官簡報。

---

## 一、為什麼不能只加一個 `if`

勝負條件現在寫死在 `_check_win_conditions()` 的兩行。真正需要「模式化」的有六處，
其中**前四處是所有模式的共同成本**，不管最後做幾個模式都得先付：

| 位置 | 現況 | 要變成 |
|---|---|---|
| `GameWorld._check_win_conditions()` | 核設施歸零／時間到 | `GameModes.check_win(world)` |
| `GameWorld._update_hud()` 的 `_lbl_obj` 與兩條基地血條 | 寫死 `structures["RUNWAY"]` / `["NUKE"]` | `GameModes.objectives(world)` 回傳 `[{label, ratio, col}]`，血條動態建立 |
| `_ai_hits_nuke()` 與 `STRIKE` 的 goal | 寫死 `NUKE_POS` / `RUNWAY_POS` | `GameModes.strike_target(world, aircraft)` |
| `Briefing._script_for(team)` | 寫死核設施劇本 | `head + terrain_lines + GameModes.brief(mode, team)` |
| `MainGame.MATCH_TIME`（常數） | 全域 600 秒 | `world.match_limit`，由模式指定 ─ **HUD 倒數、TOD_CYCLE 日夜循環都在讀它** |
| `start_match()` 的 `settings` | `tod/diff/map/seed/wx` | 加一個 `mode` key |

最後一項最單純：`settings` 已經有現成的同步機制（`cli_start_match`），加一個 key 就會自動送到每個客戶端，
跟地圖種子走同一條路，不會有兩端抽到不同模式的問題。

## 二、檔案結構

沿用專案慣例：不宣告 `class_name`、由 `load()` 動態載入。

```
GameModes.gd     模式表（中繼資料 + 簡報台詞）+ 四個行為函式
```

行為不放進字典裡（GDScript 字典塞 Callable 會很難讀），改成四個函式各自 `match mode`：

```gdscript
static func setup(world, mode) -> void        # 生成模式專屬物件
static func update(world, mode, delta) -> void
static func check_win(world, mode)            # 回傳 [team, reason] 或 null
static func objectives(world, mode) -> Array  # 目標 HUD
static func strike_target(world, a)           # AI 該打哪裡
```

模式多到一定程度再拆成 `modes/*.gd`，現在不用先拆。

## 三、模式清單

**原則：每個模式要換掉「動詞」，不是換勝負條件的數字。**
現行模式的動詞是「摧毀一個固定的東西」。再做一個「摧毀另一個建築」不算新模式 ─
玩起來會是同一件事換張皮。所以每個模式各自佔一個不同的動詞：

| 模式 | 動詞 | 勝負 | 時限 | 成本 |
|---|---|---|---|---|
| **拆除核設施**（現行） | 摧毀 | 拆掉核設施／撐過時間 | 600s | 0（搬進骨架） |
| **總統專機** | **護送 vs 攔截** | 專機飛抵撤離點＝防守方勝／被擊落＝進攻方勝 | 航程決定（約 300s） | 中～大 |
| **消耗戰** | **消耗** | 雙方共用有限復活數，先歸零的一方輸 | 無（打到某方票數見底） | 中 |
| **空域控制** | **佔據** | 累積佔領時間先到 100% 者勝，控制點每 90 秒換位置 | 420s | 小～中 |

### 為什麼是這四個

- **總統專機**：目標會動、有航路、有抵達時間 ─ 跟「定點挨打的建築」完全相反。
- **消耗戰**：現行模式是**無限復活**，死亡幾乎沒有代價（實測十分鐘出擊 12 次）。
  有限復活數會把整場的心態掉個頭：不再是一直換命換傷害，而是能不能活著回來。
  這是四個裡最省成本、但體感差異最大的一個。
- **空域控制**：從「摧毀」變成「佔據並維持」，而且控制點會換位置 ─ 戰場重心會移動，
  不像現在永遠壓在敵方基地上空。空投爭奪的圈圈、進度條、雙方同時在圈內各自減半，
  這些機制**現成就有**，改成勝負條件的成本很低。

### 刻意排除的

- **空優（比擊墜數）** ─ 原本我列了它，但它其實就是「現行模式拿掉目標」，動詞還是「擊落」，
  不符合「完全不一樣」。消耗戰涵蓋了它想要的東西，而且更有張力。
- **護送補給車隊** ─ 動詞跟總統專機重複（都是護送移動目標），做了兩個會很像。
  車隊留在現在的位置當中場事件就好。
- **空投爭奪獨立成模式** ─ 它現在是所有模式共通的中場加成事件，抽掉會讓其他模式變單調。
  空域控制是把同一套機制**放大成整場的主軸**，兩者不會同時存在（空域控制模式下關掉中場空投）。

## 四、五個設計決定（我的建議）

### 1. 陣營語意翻轉 → **不翻轉**

「保護總統專機」直覺上是防守方護航、進攻方攔截 ─ 聽起來像要對調陣營。**不要這樣做**：
出生點（進攻方＝航艦彈射、防守方＝陸上跑道）、陸軍防空歸屬、護航艦隊歸屬全部是寫死綁陣營的，
對調等於重寫世界生成。

改成這樣就完全不用動生成邏輯：

> 總統專機從防守方腹地起飛，沿航路飛向地圖外緣撤離。
> **進攻方要擊落它，防守方要護送它離場。**

進攻方仍然是進攻、防守方仍然是防守，語意一致，零改動。

### 2. 模式綁地圖 → **白名單**

專機需要一條航路。大堤頓與高山的垂直度太高，專機（用同一套 AI FSM）很可能撞山 ─
我們才剛修完 AI 撞山，不要立刻用一個必撞的物件把它打回原形。

建議：抽地圖之後再從「與該地圖相容的模式」裡抽。專機模式排除 `MAP_ALPINE` / `MAP_TETON`。

### 3. 實作順序 → **骨架 → 消耗戰 → 空域控制 → 總統專機**

骨架是共同成本，做完之後先做**消耗戰**驗證它：
消耗戰不需要任何新的 3D 物件，只動復活邏輯與一個票數 HUD ─
可以用最低的成本把整套骨架（勝負判定、目標 HUD、簡報、開賽同步）走一遍。
骨架確定通了，再做需要新物件的空域控制與總統專機。

反過來先做專機的話，骨架的 bug 會跟專機的 bug 混在一起，很難分。

### 4. 隨機 vs 房主指定 → **兩者都要**（已確認）

預設隨機，房間畫面給房主一個「指定模式」選項。
地圖目前是「一律隨機、玩家不可選」（刻意設計），但模式不一樣 ─
**測試與練習非常需要能鎖定**，不然要驗某個模式得一直重開到抽中為止。
`AUTOPLAY_MODE` 環境變數也要加，自動化測試才驗得了。

### 5. 時限 → **每個模式自帶**

`MATCH_TIME` 要從常數變成 `world.match_limit`。注意它不只是倒數顯示 ─
日夜循環模式（`TOD_CYCLE`）是用 `match_time / MATCH_TIME` 算太陽角度的，
漏改的話短模式會變成十分鐘的日夜只跑完一半。

## 五、總統專機的細節（最貴的那個）

- **機體**：沿用 `AircraftMesh` 的轟炸機外型放大，塗裝用防守方色，血量高（約 800）但不還手
- **航路**：從跑道起飛 → 兩個轉折點 → 地圖邊緣撤離點，用 `match_time` 推算位置（跟陸軍同一招，**不需要任何同步封包**）
- **不用 AI FSM**：直接照航路飛，不做迴避 ─ 這樣就完全不會有撞山問題，航路本身在生成時避開地形即可
- **勝負**：抵達撤離點＝防守方勝；HP 歸零＝進攻方勝；兩者都沒發生就打到時限
- **AI**：進攻方 `strike_target` 指向專機；防守方多一個 `ESCORT` 狀態，繞著專機巡邏
- **HUD**：目標列顯示「專機 HP％ ／ 距離撤離點 ％」兩條

## 五之二、消耗戰與空域控制的細節

### 消耗戰

- 每隊一個共用票數（建議 5v5 時各 25 張，AI 也吃同一池）
- 每次陣亡扣一張；票數歸零後該隊**不再復活**，場上剩下的打完就結束
- HUD 目標列變成兩條票數條，取代基地血條
- **復活邏輯要改**：`_update_respawns()` 現在是無條件重生，要先問模式「還有票嗎」
- 跑道／核設施在這個模式裡不參與勝負，但保留為地景（不用改生成）
- 風險：AI 不知道要惜命，可能一路送頭讓比賽三分鐘就結束 ─
  票數要實測調，`AutoPlay` 正好可以用來量

### 空域控制

- 沿用空投的圈圈與進度條機制，但控制點**每 90 秒換位置**（在戰場中段隨機挑，避開兩邊基地）
- 圈內累積時間換算成 0~100%，先到者勝
- 雙方同時在圈內時各自減半（現成行為，直接沿用）
- 這個模式下關掉中場空投加成，否則兩套一樣的圈圈同時出現會很混亂
- HUD 目標列變成兩條佔領進度條 + 一個「下次換點倒數」

## 六、風險

- **HUD 目標列動態化**是骨架裡最容易出錯的一塊：現在兩條血條是在 `_build_hud()` 就建好的固定節點，
  要改成依模式建立。改壞了每個模式都會壞。
- **`MATCH_TIME` 是 `const`**，散在 `GameWorld`、`Briefing`、HUD 好幾處，要一次改乾淨。
- **簡報時間**：`BRIEF_TIME` 是 42 秒、打字速度算過「九句剛好講完」。新模式的台詞句數要控制在同一個量級，
  否則會講不完就被切掉。
